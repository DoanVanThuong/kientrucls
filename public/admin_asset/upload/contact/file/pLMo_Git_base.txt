
git clone
git status
git add -N <Tenfile> //(thêm ảo để xem thay đổi dùng git diff)  ->git diff 
gut fetch   //đồng bộ repo local với repo trên github
git add <tenthumuc,tenfile > //them tat ca file   -> git add -A
git push origin <tennhanh>

git commit -a -m"commit không cần add"
git log
git revert  <idlog>  //quay về thời điểm
git reset <tenfile> //reset ve luc truoc khi add
git reset HEAD~ //reser ve luc truoc khi commit
ESC -> :q   //tro ve man hinh chính
git branch<tennhanh>       //tạo nhánh mới
git checkout <tennhanh> //nhảy qua nhanh đó

-> git checkout  -b <tennhanh> // tạo nhánh xong nhảy vô nhánh luôn

150KB/1s ->website tĩnh
nano <tenfile> //sữa file   :qw -> lưu và thoát

1.tạo repository trên github
2.các thành viên clone về git clone <url>
3.mỗi thành viên tạo nhánh  mới
4. chỉnh sửa (add. commit )
5.git push orign <tennhanh>
6. trưởng nhóm merge vào nhánh master
7.các thành viên dùng lệnh pull về làm tiếp
8.quay về bước 4


	